const hre = require('hardhat')
const {expect} = require('chai')
const {loadFixture} = require('@nomicfoundation/hardhat-toolbox/network-helpers')

describe("Integration test", function(){
   function deploy(){
      const [deployer, user1] = await 
   }
})